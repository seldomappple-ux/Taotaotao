# v1.3.2 主线提升实施计划

这份文档记录 `1.3.2` 提升为当前主线的执行步骤。

## 目标

把当前仓库的正式版本、主线入口和下一轮承接索引统一到 `1.3.2`。

## 实施步骤

### 1. 更新机器真源

- `.agents/profile.yaml` 的 `project_version`
- `pyproject.toml` 的 `[project].version`
- `vibe_governance/resources/release-manifest.yaml` 的 `version`
- `vibe_governance.__version__`

### 2. 更新人读主线入口

- `README.md`
- `CHANGELOG.md`
- `docs/UPGRADE_SUMMARY.md`
- `docs/NEXT_ITERATION_BASELINE.md`
- `docs/DELTA_DECISIONS.md`

### 3. 新增版本文档

- `docs/upgrades/v1.3.2-summary.md`
- `docs/upgrades/v1.3.2-plan.md`
- `.agents/progress/entries/2026/2026-06-02-1.md`

### 4. 重新生成受管输出

运行:

```bash
python -m vibe_governance render --target .
```

### 5. 验证

运行:

```bash
python -m vibe_governance validate --target .
python -m unittest discover -s tests -v
```

## 完成标准

- 当前正式版本号显示为 `1.3.2`
- 受管输出头信息显示为 `vibe-governance 1.3.2`
- `UPGRADE_SUMMARY.md` 明确 `v1.3.2` 是当前主线
- `NEXT_ITERATION_BASELINE.md` 的当前迭代和仓库版本都指向 `1.3.2`
- `render`、`validate` 和单元测试通过
