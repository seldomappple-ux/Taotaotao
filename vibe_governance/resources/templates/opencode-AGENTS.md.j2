# OPENCODE AGENTS

This file adapts the repository governance stack to opencode-compatible tooling. The canonical policy lives in `AGENTS.md` and `.agents/`.

{% for rule in rules -%}
- `{{ rule.rule_id }}`: {{ rule.body }}
{% endfor %}
