---
page_id: 20260702-1
date: 2026-07-02
title: Resolve committed merge conflicts and restore bootstrap workflow
status: draft
related_commit_message: "fix(governance): resolve committed merge conflicts and restore bootstrap"
related_commit_hash: ""
upstream_reference: ""
keywords:
  - governance
  - conflict
  - bootstrap
  - validation
---
本次修复当前 `main` 中已经提交进来的 Git 冲突标记, 让 `vibe_governance` CLI 从不可导入状态恢复为可运行状态。

修复口径:

- 以 `1.3.2` 主线作为治理底座
- 保留 `init` 作为底层兼容入口
- 恢复 `bootstrap` 作为新项目推荐入口
- 恢复 `smoke` 作为最短端到端回归命令
- 同步根 README、QUICKSTART 和嵌入式指南中的入口口径

验证结果:

- `python -m vibe_governance validate --target .` 通过
- `python -m vibe_governance smoke --target . --type embedded --name final-smoke` 通过
- `python -m unittest discover -s tests -v` 通过, 共 22 项
- 已确认仓库内无 `<<<<<<<` / `=======` / `>>>>>>>` 冲突标记
